# Server SMTP (obbligatorio per invio email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tua@email.com
# Usa una "password per app" se hai 2FA
SMTP_PASS=password-app
SMTP_FROM=tua@email.com

# Destinatario report (o usa --email-to via CLI)
EMAIL_TO=report@azienda.it
